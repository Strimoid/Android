query ContentsQuery($cursor: String) {
    contents(cursor: $cursor) {
        id
        createdAt
        title
        thumbnail
        description
        url
        commentsCount
        group {
            name
        }
        user {
            name
        }
    }
}
